The network files in this directory are in a **DRAFT** state and should **NOT** be considered Task Force outputs. For example, "example_ieee13.json" is NOT intended to represent the IEEE 13 Bus Test network in the BMOPF format (attempting to compare these models will result in a failed comparison). Instead, these files are for illustrative purposes as networks that conform to the (**DRAFT**) BMOPF JSON schema.