The canonical BMOPF JSON Schema is `../schema/bmopf.json`. `draft_bmopf_schema.json` is kept as a synced copy for existing users.

The schema is **NOT** the final schema for the math and data model as a Task Force output, it is in draft state and is subject to change.
